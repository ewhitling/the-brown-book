[project]
name = "the-brown-book"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "ao",
    "pydantic>=2.13.4",
    "pyyaml>=6.0.3",
]

[tool.uv.sources]
ao = { git = "https://github.com/ewhitling/eww-ao.git" }
